# ActionFlix Release & Update Architecture

## Release Process Flow

```
Developer
    ↓
Run: .\release.ps1 -Version "1.1.0" -CreateGitHubRelease
    ↓
┌─────────────────────────────────────────┐
│  1. Validate Environment                │
│     • Git repository check              │
│     • Clean working directory           │
│     • Version format validation         │
└─────────────────────────────────────────┘
    ↓
┌─────────────────────────────────────────┐
│  2. Update Version                      │
│     • Backup package.json               │
│     • Update version field              │
└─────────────────────────────────────────┘
    ↓
┌─────────────────────────────────────────┐
│  3. Build Application                   │
│     • npm install                       │
│     • npm run build (Windows/macOS)     │
│     • Create installers                 │
└─────────────────────────────────────────┘
    ↓
┌─────────────────────────────────────────┐
│  4. Git Operations                      │
│     • Commit version changes            │
│     • Create tag (v1.1.0)               │
│     • Push to GitHub                    │
└─────────────────────────────────────────┘
    ↓
┌─────────────────────────────────────────┐
│  5. GitHub Release                      │
│     • Create release v1.1.0             │
│     • Upload installers                 │
│     • Generate release notes            │
└─────────────────────────────────────────┘
    ↓
    ✅ Release Complete!
```

---

## Auto-Update Flow

```
End User's Computer
    ↓
ActionFlix App Launches
    ↓
Wait 3 seconds (UI loads)
    ↓
┌─────────────────────────────────────────┐
│  electron-updater                       │
│  Checks GitHub Releases                 │
└─────────────────────────────────────────┘
    ↓
    │─────────────────┬─────────────────│
    ↓                 ↓                 ↓
No Update      Update Found!    Check Failed
    ↓                 ↓                 ↓
Continue      ┌──────────────┐   Silent Fail
Using App     │ Show Popup   │   (Continue)
              │ "Update      │
              │  Available"  │
              └──────────────┘
                     ↓
              User Clicks
              "Download"
                     ↓
              ┌──────────────┐
              │  Download    │
              │  Progress    │
              │  Bar Shows   │
              └──────────────┘
                     ↓
              ┌──────────────┐
              │  Downloaded  │
              │  Complete    │
              └──────────────┘
                     ↓
              User Clicks
              "Install"
                     ↓
              ┌──────────────┐
              │  Quit App    │
              │  Install     │
              │  Relaunch    │
              └──────────────┘
                     ↓
              New Version Running!
                     ↓
              ┌──────────────────────┐
              │  User Data Preserved │
              │  • API Keys          │
              │  • Collections       │
              │  • Settings          │
              └──────────────────────┘
```

---

## System Architecture

```
┌─────────────────────────────────────────────────────────────┐
│                     GitHub Repository                        │
│  https://github.com/wullemsb/ActionFlix                     │
│                                                              │
│  Releases:                                                   │
│  • v1.0.0 (ActionFlix Setup 1.0.0.exe, .dmg files)         │
│  • v1.1.0 (ActionFlix Setup 1.1.0.exe, .dmg files)         │
│  • v1.2.0 (Latest)                                          │
└─────────────────────────────────────────────────────────────┘
                           ↑
                           │ HTTP/HTTPS
                           │ (electron-updater)
                           ↓
┌─────────────────────────────────────────────────────────────┐
│                    User's Computer                           │
│                                                              │
│  ┌────────────────────────────────────────────────┐         │
│  │           ActionFlix Application               │         │
│  │                                                │         │
│  │  ┌──────────────────────────────────────────┐ │         │
│  │  │  Main Process (main.js)                  │ │         │
│  │  │  • electron-updater                      │ │         │
│  │  │  • Update checks                         │ │         │
│  │  │  • Download/Install                      │ │         │
│  │  └──────────────────────────────────────────┘ │         │
│  │                    ↕ IPC                      │         │
│  │  ┌──────────────────────────────────────────┐ │         │
│  │  │  Renderer Process (renderer.js)          │ │         │
│  │  │  • Update UI notifications               │ │         │
│  │  │  • Progress display                      │ │         │
│  │  │  • User interaction                      │ │         │
│  │  └──────────────────────────────────────────┘ │         │
│  └────────────────────────────────────────────────┘         │
│                                                              │
│  ┌────────────────────────────────────────────────┐         │
│  │         User Data (Persistent)                 │         │
│  │  Windows: %APPDATA%\actionflix\                │         │
│  │  macOS: ~/Library/Application Support/actionflix/│         │
│  │                                                │         │
│  │  • config.json (API keys, settings)           │         │
│  │  • movies.json (saved collections)            │         │
│  │  • top10.json (Top 10 collections)            │         │
│  └────────────────────────────────────────────────┘         │
│                  ↑                                           │
│                  │ NEVER DELETED ON UPDATE                   │
│                  └───────────────────────────────            │
└─────────────────────────────────────────────────────────────┘
```

---

## Data Flow Diagram

```
┌──────────────────┐
│  Release Script  │
│   (release.ps1)  │
└────────┬─────────┘
         │
         ↓
┌──────────────────────────────────────┐
│  1. Update package.json              │
│     "version": "1.1.0"               │
└────────┬─────────────────────────────┘
         ↓
┌──────────────────────────────────────┐
│  2. electron-builder                 │
│     Reads package.json               │
│     Creates installers               │
└────────┬─────────────────────────────┘
         ↓
┌──────────────────────────────────────┐
│  3. GitHub Release                   │
│     Tag: v1.1.0                      │
│     Assets: .exe, .dmg files         │
└────────┬─────────────────────────────┘
         │
         ↓
┌──────────────────────────────────────┐
│  4. electron-updater (in user's app) │
│     GET github.com/releases/latest   │
└────────┬─────────────────────────────┘
         │
         ↓
┌──────────────────────────────────────┐
│  5. Compare Versions                 │
│     Current: 1.0.0                   │
│     Latest: 1.1.0 → Update Available!│
└────────┬─────────────────────────────┘
         │
         ↓
┌──────────────────────────────────────┐
│  6. Download Installer               │
│     Show progress to user            │
└────────┬─────────────────────────────┘
         │
         ↓
┌──────────────────────────────────────┐
│  7. Install & Relaunch               │
│     Old version → New version        │
│     User data preserved              │
└──────────────────────────────────────┘
```

---

## File Structure

```
ActionFlix/
│
├── release.ps1                    # 🚀 Release automation script
│
├── RELEASE.md                     # 📖 Complete release guide
├── RELEASE_QUICKREF.md           # 📋 Quick reference
├── IMPLEMENTATION.md             # 🔧 Technical details
├── SETUP_COMPLETE.md             # ✅ This summary
│
├── electron-app/
│   ├── package.json              # 📦 Version & build config
│   │   ├── version: "1.0.0"     # Updated by release script
│   │   ├── dependencies         # electron-updater
│   │   └── build                # electron-builder config
│   │
│   ├── main.js                   # ⚡ Main process
│   │   ├── autoUpdater          # Update checks
│   │   ├── IPC handlers         # Update commands
│   │   └── Update events        # available/downloaded
│   │
│   ├── preload.js                # 🌉 IPC bridge
│   │   ├── checkForUpdates()
│   │   ├── downloadUpdate()
│   │   ├── installUpdate()
│   │   └── onUpdate* listeners
│   │
│   ├── renderer.js               # 🎨 Frontend logic
│   │   ├── initAutoUpdater()
│   │   ├── showUpdateNotification()
│   │   └── Update UI handlers
│   │
│   ├── styles.css                # 💅 UI styling
│   │   └── .update-notification
│   │
│   └── build/
│       └── entitlements.mac.plist # 🍎 macOS permissions
│
└── dist/                         # 📦 Build outputs (gitignored)
    ├── ActionFlix Setup 1.0.0.exe
    ├── ActionFlix-1.0.0.dmg
    └── ActionFlix-1.0.0-arm64.dmg
```

---

## Component Interaction

```
┌─────────────────────────────────────────────────────────────┐
│                        User Action                           │
│                "Transform movie" or "Open Top 10"            │
└───────────────────────┬─────────────────────────────────────┘
                        │
                        ↓
┌─────────────────────────────────────────────────────────────┐
│                   Renderer Process                           │
│  • Handle UI interaction                                     │
│  • Show loading states                                       │
│  • Display results                                           │
└───────────────────────┬─────────────────────────────────────┘
                        │ IPC (contextBridge)
                        ↓
┌─────────────────────────────────────────────────────────────┐
│                    Main Process                              │
│  • OpenAI API calls                                          │
│  • TMDB API calls                                            │
│  • File system operations                                    │
│  • Auto-update checks (background)                           │
└───────────────────────┬─────────────────────────────────────┘
                        │
         ┌──────────────┼──────────────┐
         ↓              ↓              ↓
┌────────────┐  ┌──────────────┐  ┌────────────────┐
│  OpenAI    │  │  electron    │  │  electron      │
│  API       │  │  -store      │  │  -updater      │
└────────────┘  └──────────────┘  └────────────────┘
   (Transform)   (Save data)       (Update app)
```

---

This architecture ensures:
- ✅ **Automated releases** - One command
- ✅ **Seamless updates** - Zero user friction
- ✅ **Data preservation** - 100% safe
- ✅ **Beautiful UX** - Netflix-styled UI
- ✅ **Multi-platform** - Windows & macOS
